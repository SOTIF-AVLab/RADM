intersection environment defined by intersection_env.py


run iqn: python train.py

Note: there are several networks, i.e., dqn, iqn, ensemble iqn 
        but the current evironmen is only suitable for iqn. The corresponding parameters for networks selection is fixed.
